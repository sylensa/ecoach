import 'package:flutter/material.dart';

class BundleItemWidget extends StatelessWidget {
  const BundleItemWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}